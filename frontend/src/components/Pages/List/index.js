import React, { Component } from 'react'

import Layout from '../../Layout'

export default class PagesList extends Component {
  render () {
    return (
      <Layout>Pages List</Layout>
    )
  }
}
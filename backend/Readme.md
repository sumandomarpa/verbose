# Backend for Ratecity CMS

### Installation Guide
 - `mkdir -p postgres/data/`
 - `yarn install`
 - `docker-compose up -d`
 - `yarn global add prisma` If prisma is not installed already
 - `prisma deploy`
 - `yarn start`